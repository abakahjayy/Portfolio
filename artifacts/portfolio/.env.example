# URL of the FullBackendd API. Leave unset to fetch relative to the
# frontend's own origin instead (useful if both are served from the same
# domain/proxy in production).
VITE_API_URL=http://localhost:7004
